{
  "name": "badger_community_subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ antonyip/badger_community_subgraph",
    "create-local": "graph create --node http://localhost:8020/ antonyip/badger_community_subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ antonyip/badger_community_subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 antonyip/badger_community_subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
